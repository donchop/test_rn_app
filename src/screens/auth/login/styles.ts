import { ViewStyle } from 'react-native'
import { spacing } from 'src/theme'

export const LOADING_CTR: ViewStyle = {
  flex: 1,
  justifyContent: 'center',
  alignItems: 'center',
}

export const CONTAINER: ViewStyle = {
  flex: 1,
  justifyContent: 'center',
  paddingHorizontal: spacing[3],
  gap: spacing[3],
}
